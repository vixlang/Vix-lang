fn mix_35(a: i32, b: i32): i32
{
    let left = (a + 72) * 3
    let right = (b + 70) * 2
    return (left - right + 4) % 251
}

fn main(): i32
{
    let raw = mix_35(42, 16)
    if (raw < 0) { return raw + 251 }
    return raw
}
