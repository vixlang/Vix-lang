fn mix_66(a: i32, b: i32): i32
{
    let left = (a + 87) * 3
    let right = (b + 63) * 2
    return (left - right + 12) % 251
}

fn main(): i32
{
    let raw = mix_66(73, 24)
    if (raw < 0) { return raw + 251 }
    return raw
}
