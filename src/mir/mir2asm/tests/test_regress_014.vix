fn mix_14(a: i32, b: i32): i32
{
    let left = (a + 90) * 3
    let right = (b + 69) * 2
    return (left - right + 20) % 251
}

fn main(): i32
{
    let raw = mix_14(21, 18)
    if (raw < 0) { return raw + 251 }
    return raw
}
