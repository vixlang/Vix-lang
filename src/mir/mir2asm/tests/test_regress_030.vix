fn mix_30(a: i32, b: i32): i32
{
    let left = (a + 7) * 3
    let right = (b + 74) * 2
    return (left - right + 75) % 251
}

fn main(): i32
{
    let raw = mix_30(37, 11)
    if (raw < 0) { return raw + 251 }
    return raw
}
