fn mix_31(a: i32, b: i32): i32
{
    let left = (a + 20) * 3
    let right = (b + 2) * 2
    return (left - right + 11) % 251
}

fn main(): i32
{
    let raw = mix_31(38, 12)
    if (raw < 0) { return raw + 251 }
    return raw
}
