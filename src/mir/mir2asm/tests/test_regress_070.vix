fn mix_70(a: i32, b: i32): i32
{
    let left = (a + 42) * 3
    let right = (b + 42) * 2
    return (left - right + 5) % 251
}

fn main(): i32
{
    let raw = mix_70(77, 5)
    if (raw < 0) { return raw + 251 }
    return raw
}
