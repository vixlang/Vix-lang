fn mix_18(a: i32, b: i32): i32
{
    let left = (a + 45) * 3
    let right = (b + 48) * 2
    return (left - right + 13) % 251
}

fn main(): i32
{
    let raw = mix_18(25, 22)
    if (raw < 0) { return raw + 251 }
    return raw
}
