fn mix_1(a: i32, b: i32): i32
{
    let left = (a + 18) * 3
    let right = (b + 26) * 2
    return (left - right + 22) % 251
}

fn main(): i32
{
    let raw = mix_1(8, 5)
    if (raw < 0) { return raw + 251 }
    return raw
}
