fn mix_65(a: i32, b: i32): i32
{
    let left = (a + 74) * 3
    let right = (b + 46) * 2
    return (left - right + 76) % 251
}

fn main(): i32
{
    let raw = mix_65(72, 23)
    if (raw < 0) { return raw + 251 }
    return raw
}
