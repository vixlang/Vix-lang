fn mix_97(a: i32, b: i32): i32
{
    let left = (a + 5) * 3
    let right = (b + 56) * 2
    return (left - right + 20) % 251
}

fn main(): i32
{
    let raw = mix_97(104, 9)
    if (raw < 0) { return raw + 251 }
    return raw
}
