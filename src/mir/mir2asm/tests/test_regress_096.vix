fn mix_96(a: i32, b: i32): i32
{
    let left = (a + 89) * 3
    let right = (b + 39) * 2
    return (left - right + 1) % 251
}

fn main(): i32
{
    let raw = mix_96(103, 8)
    if (raw < 0) { return raw + 251 }
    return raw
}
