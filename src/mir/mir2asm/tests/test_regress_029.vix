fn mix_29(a: i32, b: i32): i32
{
    let left = (a + 91) * 3
    let right = (b + 57) * 2
    return (left - right + 56) % 251
}

fn main(): i32
{
    let raw = mix_29(36, 10)
    if (raw < 0) { return raw + 251 }
    return raw
}
