fn mix_87(a: i32, b: i32): i32
{
    let left = (a + 69) * 3
    let right = (b + 64) * 2
    return (left - right + 79) % 251
}

fn main(): i32
{
    let raw = mix_87(94, 22)
    if (raw < 0) { return raw + 251 }
    return raw
}
