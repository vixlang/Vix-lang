fn mix_51(a: i32, b: i32): i32
{
    let left = (a + 86) * 3
    let right = (b + 75) * 2
    return (left - right + 59) % 251
}

fn main(): i32
{
    let raw = mix_51(58, 9)
    if (raw < 0) { return raw + 251 }
    return raw
}
