fn mix_3(a: i32, b: i32): i32
{
    let left = (a + 44) * 3
    let right = (b + 60) * 2
    return (left - right + 60) % 251
}

fn main(): i32
{
    let raw = mix_3(10, 7)
    if (raw < 0) { return raw + 251 }
    return raw
}
