fn mix_2(a: i32, b: i32): i32
{
    let left = (a + 31) * 3
    let right = (b + 43) * 2
    return (left - right + 41) % 251
}

fn main(): i32
{
    let raw = mix_2(9, 6)
    if (raw < 0) { return raw + 251 }
    return raw
}
