fn mix_69(a: i32, b: i32): i32
{
    let left = (a + 29) * 3
    let right = (b + 25) * 2
    return (left - right + 69) % 251
}

fn main(): i32
{
    let raw = mix_69(76, 4)
    if (raw < 0) { return raw + 251 }
    return raw
}
