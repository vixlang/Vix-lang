fn mix_15(a: i32, b: i32): i32
{
    let left = (a + 6) * 3
    let right = (b + 86) * 2
    return (left - right + 39) % 251
}

fn main(): i32
{
    let raw = mix_15(22, 19)
    if (raw < 0) { return raw + 251 }
    return raw
}
