fn mix_78(a: i32, b: i32): i32
{
    let left = (a + 49) * 3
    let right = (b + 0) * 2
    return (left - right + 74) % 251
}

fn main(): i32
{
    let raw = mix_78(85, 13)
    if (raw < 0) { return raw + 251 }
    return raw
}
