fn mix_46(a: i32, b: i32): i32
{
    let left = (a + 21) * 3
    let right = (b + 79) * 2
    return (left - right + 47) % 251
}

fn main(): i32
{
    let raw = mix_46(53, 4)
    if (raw < 0) { return raw + 251 }
    return raw
}
