fn mix_72(a: i32, b: i32): i32
{
    let left = (a + 68) * 3
    let right = (b + 76) * 2
    return (left - right + 43) % 251
}

fn main(): i32
{
    let raw = mix_72(79, 7)
    if (raw < 0) { return raw + 251 }
    return raw
}
