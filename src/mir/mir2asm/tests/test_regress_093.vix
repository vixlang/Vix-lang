fn mix_93(a: i32, b: i32): i32
{
    let left = (a + 50) * 3
    let right = (b + 77) * 2
    return (left - right + 27) % 251
}

fn main(): i32
{
    let raw = mix_93(100, 5)
    if (raw < 0) { return raw + 251 }
    return raw
}
