fn mix_39(a: i32, b: i32): i32
{
    let left = (a + 27) * 3
    let right = (b + 49) * 2
    return (left - right + 80) % 251
}

fn main(): i32
{
    let raw = mix_39(46, 20)
    if (raw < 0) { return raw + 251 }
    return raw
}
