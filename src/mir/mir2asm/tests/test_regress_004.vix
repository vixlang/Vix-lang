fn mix_4(a: i32, b: i32): i32
{
    let left = (a + 57) * 3
    let right = (b + 77) * 2
    return (left - right + 79) % 251
}

fn main(): i32
{
    let raw = mix_4(11, 8)
    if (raw < 0) { return raw + 251 }
    return raw
}
