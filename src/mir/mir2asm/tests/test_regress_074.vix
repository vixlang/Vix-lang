fn mix_74(a: i32, b: i32): i32
{
    let left = (a + 94) * 3
    let right = (b + 21) * 2
    return (left - right + 81) % 251
}

fn main(): i32
{
    let raw = mix_74(81, 9)
    if (raw < 0) { return raw + 251 }
    return raw
}
