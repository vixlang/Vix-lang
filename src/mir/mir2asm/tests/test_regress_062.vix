fn mix_62(a: i32, b: i32): i32
{
    let left = (a + 35) * 3
    let right = (b + 84) * 2
    return (left - right + 19) % 251
}

fn main(): i32
{
    let raw = mix_62(69, 20)
    if (raw < 0) { return raw + 251 }
    return raw
}
