fn mix_90(a: i32, b: i32): i32
{
    let left = (a + 11) * 3
    let right = (b + 26) * 2
    return (left - right + 53) % 251
}

fn main(): i32
{
    let raw = mix_90(97, 25)
    if (raw < 0) { return raw + 251 }
    return raw
}
