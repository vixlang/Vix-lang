fn mix_38(a: i32, b: i32): i32
{
    let left = (a + 14) * 3
    let right = (b + 32) * 2
    return (left - right + 61) % 251
}

fn main(): i32
{
    let raw = mix_38(45, 19)
    if (raw < 0) { return raw + 251 }
    return raw
}
