fn mix_11(a: i32, b: i32): i32
{
    let left = (a + 51) * 3
    let right = (b + 18) * 2
    return (left - right + 46) % 251
}

fn main(): i32
{
    let raw = mix_11(18, 15)
    if (raw < 0) { return raw + 251 }
    return raw
}
