fn mix_63(a: i32, b: i32): i32
{
    let left = (a + 48) * 3
    let right = (b + 12) * 2
    return (left - right + 38) % 251
}

fn main(): i32
{
    let raw = mix_63(70, 21)
    if (raw < 0) { return raw + 251 }
    return raw
}
