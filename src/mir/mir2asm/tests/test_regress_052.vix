fn mix_52(a: i32, b: i32): i32
{
    let left = (a + 2) * 3
    let right = (b + 3) * 2
    return (left - right + 78) % 251
}

fn main(): i32
{
    let raw = mix_52(59, 10)
    if (raw < 0) { return raw + 251 }
    return raw
}
