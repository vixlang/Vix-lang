fn mix_43(a: i32, b: i32): i32
{
    let left = (a + 79) * 3
    let right = (b + 28) * 2
    return (left - right + 73) % 251
}

fn main(): i32
{
    let raw = mix_43(50, 24)
    if (raw < 0) { return raw + 251 }
    return raw
}
