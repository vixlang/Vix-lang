fn mix_48(a: i32, b: i32): i32
{
    let left = (a + 47) * 3
    let right = (b + 24) * 2
    return (left - right + 2) % 251
}

fn main(): i32
{
    let raw = mix_48(55, 6)
    if (raw < 0) { return raw + 251 }
    return raw
}
