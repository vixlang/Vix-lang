fn mix_49(a: i32, b: i32): i32
{
    let left = (a + 60) * 3
    let right = (b + 41) * 2
    return (left - right + 21) % 251
}

fn main(): i32
{
    let raw = mix_49(56, 7)
    if (raw < 0) { return raw + 251 }
    return raw
}
