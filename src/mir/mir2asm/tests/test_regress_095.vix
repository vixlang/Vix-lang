fn mix_95(a: i32, b: i32): i32
{
    let left = (a + 76) * 3
    let right = (b + 22) * 2
    return (left - right + 65) % 251
}

fn main(): i32
{
    let raw = mix_95(102, 7)
    if (raw < 0) { return raw + 251 }
    return raw
}
