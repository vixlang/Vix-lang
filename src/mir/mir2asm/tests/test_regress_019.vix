fn mix_19(a: i32, b: i32): i32
{
    let left = (a + 58) * 3
    let right = (b + 65) * 2
    return (left - right + 32) % 251
}

fn main(): i32
{
    let raw = mix_19(26, 23)
    if (raw < 0) { return raw + 251 }
    return raw
}
