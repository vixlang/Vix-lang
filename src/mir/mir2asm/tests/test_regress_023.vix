fn mix_23(a: i32, b: i32): i32
{
    let left = (a + 13) * 3
    let right = (b + 44) * 2
    return (left - right + 25) % 251
}

fn main(): i32
{
    let raw = mix_23(30, 4)
    if (raw < 0) { return raw + 251 }
    return raw
}
