fn mix_60(a: i32, b: i32): i32
{
    let left = (a + 9) * 3
    let right = (b + 50) * 2
    return (left - right + 64) % 251
}

fn main(): i32
{
    let raw = mix_60(67, 18)
    if (raw < 0) { return raw + 251 }
    return raw
}
