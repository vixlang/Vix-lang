fn mix_68(a: i32, b: i32): i32
{
    let left = (a + 16) * 3
    let right = (b + 8) * 2
    return (left - right + 50) % 251
}

fn main(): i32
{
    let raw = mix_68(75, 26)
    if (raw < 0) { return raw + 251 }
    return raw
}
