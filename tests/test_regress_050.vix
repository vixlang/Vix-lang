fn mix_50(a: i32, b: i32): i32
{
    let left = (a + 73) * 3
    let right = (b + 58) * 2
    return (left - right + 40) % 251
}

fn main(): i32
{
    let raw = mix_50(57, 8)
    if (raw < 0) { return raw + 251 }
    return raw
}
