fn main(): i32 {
    let a = (7, 2.5)
    let b = ((1, 2), (3, 4))
    let c = [(5, 6), (7, 8), (9, 10)]
    let mut total = a.0 + b.0.0 + b.1.1
    for (i in 0 .. c.length) {
        total = total + c[i].0 + c[i].1
    }
    return total
}
