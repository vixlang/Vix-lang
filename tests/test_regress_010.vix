fn mix_10(a: i32, b: i32): i32
{
    let left = (a + 38) * 3
    let right = (b + 1) * 2
    return (left - right + 27) % 251
}

fn main(): i32
{
    let raw = mix_10(17, 14)
    if (raw < 0) { return raw + 251 }
    return raw
}
