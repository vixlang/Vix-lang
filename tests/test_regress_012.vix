fn mix_12(a: i32, b: i32): i32
{
    let left = (a + 64) * 3
    let right = (b + 35) * 2
    return (left - right + 65) % 251
}

fn main(): i32
{
    let raw = mix_12(19, 16)
    if (raw < 0) { return raw + 251 }
    return raw
}
