fn mix_57(a: i32, b: i32): i32
{
    let left = (a + 67) * 3
    let right = (b + 88) * 2
    return (left - right + 7) % 251
}

fn main(): i32
{
    let raw = mix_57(64, 15)
    if (raw < 0) { return raw + 251 }
    return raw
}
