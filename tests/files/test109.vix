fn mix_86(a: i32, b: i32): i32
{
    let left = (a + 56) * 3
    let right = (b + 47) * 2
    return (left - right + 60) % 251
}

fn main(): i32
{
    let raw = mix_86(93, 21)
    if (raw < 0) { return raw + 251 }
    return raw
}
