fn mix_8(a: i32, b: i32): i32
{
    let left = (a + 12) * 3
    let right = (b + 56) * 2
    return (left - right + 72) % 251
}

fn main(): i32
{
    let raw = mix_8(15, 12)
    if (raw < 0) { return raw + 251 }
    return raw
}
