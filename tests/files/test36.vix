fn mix_13(a: i32, b: i32): i32
{
    let left = (a + 77) * 3
    let right = (b + 52) * 2
    return (left - right + 1) % 251
}

fn main(): i32
{
    let raw = mix_13(20, 17)
    if (raw < 0) { return raw + 251 }
    return raw
}
