fn mix_0(a: i32, b: i32): i32
{
    let left = (a + 5) * 3
    let right = (b + 9) * 2
    return (left - right + 3) % 251
}

fn main(): i32
{
    let raw = mix_0(7, 4)
    if (raw < 0) { return raw + 251 }
    return raw
}
