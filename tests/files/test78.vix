fn mix_55(a: i32, b: i32): i32
{
    let left = (a + 41) * 3
    let right = (b + 54) * 2
    return (left - right + 52) % 251
}

fn main(): i32
{
    let raw = mix_55(62, 13)
    if (raw < 0) { return raw + 251 }
    return raw
}
