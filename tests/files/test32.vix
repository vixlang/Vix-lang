fn mix_9(a: i32, b: i32): i32
{
    let left = (a + 25) * 3
    let right = (b + 73) * 2
    return (left - right + 8) % 251
}

fn main(): i32
{
    let raw = mix_9(16, 13)
    if (raw < 0) { return raw + 251 }
    return raw
}
