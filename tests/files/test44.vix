fn mix_21(a: i32, b: i32): i32
{
    let left = (a + 84) * 3
    let right = (b + 10) * 2
    return (left - right + 70) % 251
}

fn main(): i32
{
    let raw = mix_21(28, 25)
    if (raw < 0) { return raw + 251 }
    return raw
}
