fn mix_42(a: i32, b: i32): i32
{
    let left = (a + 66) * 3
    let right = (b + 11) * 2
    return (left - right + 54) % 251
}

fn main(): i32
{
    let raw = mix_42(49, 23)
    if (raw < 0) { return raw + 251 }
    return raw
}
