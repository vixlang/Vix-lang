fn mix_79(a: i32, b: i32): i32
{
    let left = (a + 62) * 3
    let right = (b + 17) * 2
    return (left - right + 10) % 251
}

fn main(): i32
{
    let raw = mix_79(86, 14)
    if (raw < 0) { return raw + 251 }
    return raw
}
