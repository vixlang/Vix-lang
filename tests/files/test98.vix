fn mix_75(a: i32, b: i32): i32
{
    let left = (a + 10) * 3
    let right = (b + 38) * 2
    return (left - right + 17) % 251
}

fn main(): i32
{
    let raw = mix_75(82, 10)
    if (raw < 0) { return raw + 251 }
    return raw
}
