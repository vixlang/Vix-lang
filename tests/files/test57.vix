fn mix_34(a: i32, b: i32): i32
{
    let left = (a + 59) * 3
    let right = (b + 53) * 2
    return (left - right + 68) % 251
}

fn main(): i32
{
    let raw = mix_34(41, 15)
    if (raw < 0) { return raw + 251 }
    return raw
}
