fn mix_67(a: i32, b: i32): i32
{
    let left = (a + 3) * 3
    let right = (b + 80) * 2
    return (left - right + 31) % 251
}

fn main(): i32
{
    let raw = mix_67(74, 25)
    if (raw < 0) { return raw + 251 }
    return raw
}
