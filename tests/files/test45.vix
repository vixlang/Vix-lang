fn mix_22(a: i32, b: i32): i32
{
    let left = (a + 0) * 3
    let right = (b + 27) * 2
    return (left - right + 6) % 251
}

fn main(): i32
{
    let raw = mix_22(29, 26)
    if (raw < 0) { return raw + 251 }
    return raw
}
