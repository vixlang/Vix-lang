mir_module vixc0 {
fn @main(): i32 {
    block %entry:
        %a = alloca i32
        store i32 42, %a
        %slot = alloca ptr:i32
        store ptr:i32 %a, %slot
        %p = load ptr:i32, %slot
        %x = load i32, %p
        ret i32 %x
}
}
