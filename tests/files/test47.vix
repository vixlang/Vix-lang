fn mix_24(a: i32, b: i32): i32
{
    let left = (a + 26) * 3
    let right = (b + 61) * 2
    return (left - right + 44) % 251
}

fn main(): i32
{
    let raw = mix_24(31, 5)
    if (raw < 0) { return raw + 251 }
    return raw
}
