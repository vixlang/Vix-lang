fn mix_61(a: i32, b: i32): i32
{
    let left = (a + 22) * 3
    let right = (b + 67) * 2
    return (left - right + 0) % 251
}

fn main(): i32
{
    let raw = mix_61(68, 19)
    if (raw < 0) { return raw + 251 }
    return raw
}
