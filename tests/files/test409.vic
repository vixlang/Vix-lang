mir_module vixc0 {
fn @main(): i32 {
    block %entry:
        %a = alloca i32
        store i32 42, %a
        %paddr = alloca ptr
        store ptr %a, %paddr
        %p = load ptr, %paddr
        %q = gep i32, %p, [i32 0]
        %x = load i32, %q
        ret i32 %x
}
}
