fn mix_84(a: i32, b: i32): i32
{
    let left = (a + 30) * 3
    let right = (b + 13) * 2
    return (left - right + 22) % 251
}

fn main(): i32
{
    let raw = mix_84(91, 19)
    if (raw < 0) { return raw + 251 }
    return raw
}
