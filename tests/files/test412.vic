mir_module vixc0 {
fn @sum6(%a: i32, %b: i32, %c: i32, %d: i32, %e: i32, %f: i32): i32 {
    block %entry:
        %x = add i32 %a, %b
        %y = add i32 %x, %c
        %z = add i32 %y, %d
        %q = add i32 %z, %e
        %r = add i32 %q, %f
        ret i32 %r
}
fn @main(): i32 {
    block %entry:
        %x = call @sum6(i32 1, i32 2, i32 3, i32 4, i32 5, i32 6): i32
        ret i32 %x
}
}
