fn mix_36(a: i32, b: i32): i32
{
    let left = (a + 85) * 3
    let right = (b + 87) * 2
    return (left - right + 23) % 251
}

fn main(): i32
{
    let raw = mix_36(43, 17)
    if (raw < 0) { return raw + 251 }
    return raw
}
