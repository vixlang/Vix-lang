fn mix_83(a: i32, b: i32): i32
{
    let left = (a + 17) * 3
    let right = (b + 85) * 2
    return (left - right + 3) % 251
}

fn main(): i32
{
    let raw = mix_83(90, 18)
    if (raw < 0) { return raw + 251 }
    return raw
}
