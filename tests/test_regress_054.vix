fn mix_54(a: i32, b: i32): i32
{
    let left = (a + 28) * 3
    let right = (b + 37) * 2
    return (left - right + 33) % 251
}

fn main(): i32
{
    let raw = mix_54(61, 12)
    if (raw < 0) { return raw + 251 }
    return raw
}
