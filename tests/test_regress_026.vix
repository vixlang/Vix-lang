fn mix_26(a: i32, b: i32): i32
{
    let left = (a + 52) * 3
    let right = (b + 6) * 2
    return (left - right + 82) % 251
}

fn main(): i32
{
    let raw = mix_26(33, 7)
    if (raw < 0) { return raw + 251 }
    return raw
}
