fn sink(value: i32): void {}

fn main(): i32 {
    let values = [1, 2, 3]
    for (value in values) => sink(value)
    let mut sum = 0
    for (index in 0 .. 4) => sum = sum + index
    return sum - 6
}
