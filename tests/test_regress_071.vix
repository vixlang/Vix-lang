fn mix_71(a: i32, b: i32): i32
{
    let left = (a + 55) * 3
    let right = (b + 59) * 2
    return (left - right + 24) % 251
}

fn main(): i32
{
    let raw = mix_71(78, 6)
    if (raw < 0) { return raw + 251 }
    return raw
}
