fn mix_20(a: i32, b: i32): i32
{
    let left = (a + 71) * 3
    let right = (b + 82) * 2
    return (left - right + 51) % 251
}

fn main(): i32
{
    let raw = mix_20(27, 24)
    if (raw < 0) { return raw + 251 }
    return raw
}
