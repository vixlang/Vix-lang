fn mix_5(a: i32, b: i32): i32
{
    let left = (a + 70) * 3
    let right = (b + 5) * 2
    return (left - right + 15) % 251
}

fn main(): i32
{
    let raw = mix_5(12, 9)
    if (raw < 0) { return raw + 251 }
    return raw
}
