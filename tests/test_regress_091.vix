fn mix_91(a: i32, b: i32): i32
{
    let left = (a + 24) * 3
    let right = (b + 43) * 2
    return (left - right + 72) % 251
}

fn main(): i32
{
    let raw = mix_91(98, 26)
    if (raw < 0) { return raw + 251 }
    return raw
}
