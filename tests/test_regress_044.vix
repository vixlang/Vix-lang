fn mix_44(a: i32, b: i32): i32
{
    let left = (a + 92) * 3
    let right = (b + 45) * 2
    return (left - right + 9) % 251
}

fn main(): i32
{
    let raw = mix_44(51, 25)
    if (raw < 0) { return raw + 251 }
    return raw
}
