fn mix_92(a: i32, b: i32): i32
{
    let left = (a + 37) * 3
    let right = (b + 60) * 2
    return (left - right + 8) % 251
}

fn main(): i32
{
    let raw = mix_92(99, 4)
    if (raw < 0) { return raw + 251 }
    return raw
}
