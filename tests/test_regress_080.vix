fn mix_80(a: i32, b: i32): i32
{
    let left = (a + 75) * 3
    let right = (b + 34) * 2
    return (left - right + 29) % 251
}

fn main(): i32
{
    let raw = mix_80(87, 15)
    if (raw < 0) { return raw + 251 }
    return raw
}
