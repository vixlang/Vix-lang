fn mix_59(a: i32, b: i32): i32
{
    let left = (a + 93) * 3
    let right = (b + 33) * 2
    return (left - right + 45) % 251
}

fn main(): i32
{
    let raw = mix_59(66, 17)
    if (raw < 0) { return raw + 251 }
    return raw
}
