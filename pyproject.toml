[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "vixc-tests"
version = "0.3.0"
description = "Test suite for the Vix compiler (vixc)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py", "cli.py", "errors.py", "feat.py", "fuzz.py", "regre.py", "stress.py", "test_types.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "unit: Unit tests for compiler internals",
    "integration: Integration tests (compile + run)",
    "fuzz: Fuzz testing",
    "stress: Stress and load tests",
    "cli: Command-line interface tests",
    "error: Error handling and diagnostics tests",
    "feature: Feature-specific tests",
    "slow: Tests that take a long time to run",
]
addopts = "-v --tb=short"
timeout = 60
